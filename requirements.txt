# Redrob Hackathon — rank.py uses only Python stdlib (no packages needed).
# The ranker runs fully offline: no API calls, no GPU, no heavy ML deps.
#
# Optional — only needed if you want to run the demo Flask app (demo.html backend):
flask>=3.0.0

# Optional — if you extend the ranker with local embedding models in the future:
# sentence-transformers>=3.0
# faiss-cpu>=1.8
